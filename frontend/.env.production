# Production environment — committed because NEXT_PUBLIC_ vars must be
# available at build time (Azure App Settings arrive too late for Next.js).
# The publishable key is intentionally public — never put the SECRET key here.

NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_ACTUAL_KEY_HERE
